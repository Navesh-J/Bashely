/** @type {import("tailwindcss/v3").Config} */
module.exports = {
  darkMode: 'class', // or 'media'
  content: [
    './src/**/*.{js,ts,jsx,tsx,html}'
  ],
  theme: {
    extend: {
      // custom theme tokens or overrides
    }
  },
  plugins: [],
};